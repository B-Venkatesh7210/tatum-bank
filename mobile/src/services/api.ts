/**
 * @deprecated Import from `./api.service` instead. Re-exports preserved for existing imports.
 */
export {
  TOKEN_STORAGE_KEY,
  getApiBaseUrl,
  getApiClient,
  setAuthToken,
  getAuthToken,
} from "./api.service";
